concat_objects.pl

take a list of obj-files and concatenate them to a single file. You can not just put obj-files in one file without updating the object and face ids


normalize_obj_vertices.pl

shrink or magnify obj coordinates, apply xyz shift in a way that they can be shown in a standard three.js environment.

When used without fixed parameters they are calculated from data generated and shown in file name


 
